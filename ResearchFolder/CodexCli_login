# codex cli 登入有什麼方法可以直接給予 yolo 模式 比如 codex yolo 有嗎

有，現在 Codex CLI 有 `--yolo`。

等同於：

```bash
codex --dangerously-bypass-approvals-and-sandbox
```

簡寫就是：

```bash
codex --yolo
```

它的意思是「跳過 approval prompts 與 sandbox」，官方也標註危險，只建議在隔離環境使用。([OpenAI 開發者][1])

常見用法：

```bash
codex --yolo
```

或直接帶任務：

```bash
codex --yolo "幫我修正這個專案的 build error"
```

比較安全的接近模式是 Full Access / Auto：

```bash
codex --full-auto
```

官方說明：`Auto` 預設會允許在工作目錄內讀檔、修改、執行命令；但碰到工作區外或網路仍會詢問。`Full Access` 則可跨機器與網路操作且不詢問，要謹慎使用。([OpenAI 開發者][2])

也可以在 `~/.codex/config.toml` 設定 approval / sandbox，例如 `approval_policy = "never"` 這類設定；官方 config reference 有列出 `approval_policy` 可用值。([OpenAI 開發者][3])

建議你如果只是想少按確認，先用：

```bash
codex --full-auto
```

真的在 Docker、VM、測試 repo 裡，再用：

```bash
codex --yolo
```

[1]: https://developers.openai.com/codex/cli/reference?utm_source=chatgpt.com "Command line options – Codex CLI"
[2]: https://developers.openai.com/codex/cli/features?utm_source=chatgpt.com "Codex CLI features"
[3]: https://developers.openai.com/codex/config-reference?utm_source=chatgpt.com "Configuration Reference – Codex"
